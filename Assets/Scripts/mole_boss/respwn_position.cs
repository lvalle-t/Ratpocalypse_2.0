using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class respwn_position : MonoBehaviour
{

    //private Vector3 respawnPosition;
    //public GameObject nextSceneCollider;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    //respawnPosition = transform.position;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    respawnPosition = transform.position;
    //    nextSceneCollider.transform.position = new Vector2(transform.position.x, nextSceneCollider.transform.position.y);
    //}

    //private void FixedUpdate()
    //{
    //    respawnPosition = transform.position;
    //}

    //private void OnTriggerEnter2D(Collider2D collision)
    //{
    //    if (collision.gameObject.CompareTag("PreviousScene"))           //collision.tag == "PreviousScene")
    //    {
    //        SceneManager.LoadScene(SceneManager.GetActiveScene().buildIndex - 1);
    //        transform.position = respawnPosition;
    //    }
    //}

    //public void ResponPos()
    //{
    //    transform.position = respawnPosition;
    //}
}
